name: Scheduled jobs

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled scheduled automation
        run: echo "Scheduled cron jobs are disabled."
         
